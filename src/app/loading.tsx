import { LoadingScreen } from "@/components/portfolio/LoadingScreen";

/**
 * Next.js route-level loading UI. Shown when the page segment is loading.
 */
export default function Loading() {
  return <LoadingScreen />;
}
