/**
 * Single source for portfolio stats — update here to keep Hero KPIs and project count in sync.
 */

export const PORTFOLIO_STATS = {
  /** Number of completed/featured projects (excludes "Coming soon" placeholders) */
  projectCount: 1,
  /** Client count display (e.g. "1+") */
  clientCount: "1+",
} as const;
